import React from "react";
import { Button } from "react-native-elements";

const MyButton = () => {
<View>
    <Button title={this.props.name} onPress={this.props.onPress}></Button>
</View>
}

export default MyButton;
